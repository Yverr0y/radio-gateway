# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific
logs/
*.log
*.pid
*.backup
*.bak
*~

# Fleet documentation — sensitive (node IPs, credentials, service internals) — never commit
SYSTEM_MANIFEST.md
hourly.md
daily.md
manager_state.json
manager_reports.jsonl

# User configurations — never commit (contains secrets: stream keys, passwords)
gateway_config.txt
sdr_channels.json
routing_config.json
routing_config.json.bak*
*.bak-*
endpoints_state.json
.transcribe_settings.json
gateway_config_local.txt
windows_audio_client.json
web_cert.pem
web_key.pem
certs/

# Audio test files — kept locally but never committed (sensitive content)
audio/

# Windows audio client diagnostic artifacts
tx_dump_*.wav
diag_raw_*.wav
diag_output.txt
startup.log

# Chrome Remote Desktop session files (system-generated, not project-related)
chrome-remote-desktop-existing-session/

# Runtime output files
tools/audio_trace.txt
tools/*_trace.txt
tools/audio_trace_*.txt
tools/stream_trace_*.txt
tools/stream_trace.txt
tools/watchdog_trace.txt

# Downloads/temp
node-*.tar.xz
*.deb
*.zip
pymumble-pymumble_py3/
serial_prog/
audio/.cache/
recordings/
tools/room-monitor-app/.gradle/
tools/room-monitor-app/app/build/
tools/room-monitor-app/build/
tools/room-monitor-app/local.properties
gateway_mcp.py.legacy

# Real fart samples — fetched by fart_player.py / setup (not committed)
audio/farts/

# Project memory — synced via a separate PRIVATE repo, never in this public one
.claude/memory/

# AllStar USRP recent-nodes list (runtime state)
usrp_recent.json
usrp2_recent.json

# Kokoro TTS ONNX models — large binaries (>300 MB), fetched by install.sh
tools/models/kokoro/
.claude/worktrees/
